# Codebase Summary

## Trạng thái repository

- Thư mục dự án `/Users/admin/ketoan16` hiện **trống** (chưa có `__manifest__.py`, chưa có mã Odoo).
- Tài liệu trong `docs/` ghi lại **phân tích & kế hoạch** cho dự án triển khai ngoài repo (Odoo 16 Enterprise + Vietnam Accounting Localization).

## Khi có mã — cần bổ sung vào tài liệu này

- Cây thư mục module (tên kỹ thuật `l10n_vn*` / custom).
- File Python/XML chính cho báo cáo bị lệch.
- Điểm vào test (`tests/`).

## Công nghệ dự kiến

- Odoo 16 Enterprise, PostgreSQL, Python 3.x theo bản Odoo 16.
